dev:
  target: dev
  outputs:
    dev:
      type: databricks
      catalog: olist_project_dev          
      schema: olist_gold
      host: dbc-73e4c67b-b212.cloud.databricks.com
      http_path: "{{ env_var('DBT_HTTP_PATH') }}" # /sql/1.0/warehouses/867f5dd25221550d
      token: "{{ env_var('DBT_TOKEN') }}" 
      threads: 4

prod:
  target: prod
  outputs:
    dev:
      type: databricks
      catalog: olist_project_prod          
      schema: olist_gold
      host: dbc-73e4c67b-b212.cloud.databricks.com
      http_path: "{{ env_var('DBT_HTTP_PATH') }}" 
      token: "{{ env_var('DBT_TOKEN') }}"
      threads: 4